type initStateProps = {
  name: string;
  level: string;
  workoutDays: Array<string>;
  myWorkouts: Array<string>;
  lastWorkouts: string;
  dailyProgress: Array<string>;
};

export default initStateProps;
